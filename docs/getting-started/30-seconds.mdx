---
id: 30-seconds
title: Install Avuru Obs and discover your first Kubernetes service map
sidebar_label: Quickstart
sidebar_position: 1
description: Install the Avuru Obs Helm chart, open the UI and verify your first services and traces on a Kubernetes cluster.
---

import {InstallSelector} from '@site/src/components';

# Install Avuru Obs and discover your first service map

Start on a test cluster. eBPF discovers supported traffic without application
changes; OpenTelemetry extends collection where you need it. Startup depends
on your cluster, image downloads and application traffic.

## 1. Check prerequisites

You need Helm, a reachable Kubernetes context, persistent storage for ClickHouse
and Linux nodes compatible with the sensor. Read the
[Kubernetes requirements](/docs/setup/kubernetes/#requirements).
eBPF collection needs node-level permissions; check your cluster's security constraints.

## 2. Install

<InstallSelector options={['Kubernetes', 'Docker', 'VM']} defaultOption="Kubernetes" />

The chart brings together the sensor, gateway, hub, UI and ClickHouse. The
example uses a [published release](https://github.com/avuruvision/avuru-obs/releases).
Docker supports local evaluation; the VM path uses your OTLP pipeline.

## 3. Open the UI

```bash
kubectl -n avuruobs port-forward svc/avuruobs-ui 8080:80
```

Open **http://localhost:8080/** and select the receiving project. Generate traffic
to a supported application: an empty map on an idle cluster is not necessarily
a collection failure.

## 4. Verify

```bash
kubectl -n avuruobs get pods
```

Check that the components are ready, then find a service on the map and a recent
trace. If ClickHouse is pending, inspect its PVC and StorageClass. If signals
are missing, check sensor permissions and logs, kernel/protocol support and
the selected project.

## 5. Send your existing OTLP

From a different Kubernetes namespace, use the complete service DNS name:

```bash
export OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://avuruobs-gateway.avuruobs.svc.cluster.local:4318/v1/traces
```

This endpoint is for OTLP/HTTP. gRPC uses port 4317 with the corresponding SDK
configuration. Read the [OTLP guide](/docs/setup/otel-bridge/) for ingest keys,
dual export and other signals.

## Next

- [Understand the service map](/docs/signals/service-map/).
- [Investigate a slow checkout](/guides/investigate-slow-checkout/) with a reproducible synthetic example.
- [Choose your modules](/docs/setup/modules/) and check their prerequisites.
