---
id: status
title: Feature status
sidebar_label: Feature status
sidebar_position: 5
description: What works in avuru obs today versus what's on the way.
---

import {StatusBadge} from '@site/src/components';

# Feature status

A snapshot of what's live today versus what's coming. avuru obs
[**v0.7.0** is released](/docs/releases) and the trunk moves fast — for the
plan see the [Roadmap](/docs/roadmap), and for shipped changes the
[Changelog](/changelog).

## Available now

| Capability | Status | Since |
|---|---|---|
| Service map — call edges **derived from traces**, health-status rings from the service-health rollup, per-edge caller-side p50/p95 latency, hover-to-focus, and shareable search/status/group filters | <StatusBadge status="shipped" /> | M1 |
| Virtual targets — the databases, caches and message brokers your services depend on, derived from their exit spans; no agent in the dependency, and a broker drawn from both ends | <StatusBadge status="shipped" /> | v0.8 |
| Map boundaries — group the graph by Kubernetes namespace or by service group, each drawn as a labelled container | <StatusBadge status="shipped" /> | v0.8 |
| Undetected peers — the far end of a connection nobody instrumented, drawn instead of discarded, and counted apart from services | <StatusBadge status="shipped" /> | v0.8 |
| Traces — search, filters (tags, order, duration, status), latency heatmap, per-operation RED overview | <StatusBadge status="shipped" /> | M1 |
| Trace viewer — timeline, spans table, flamegraph, statistics, trace graph, JSON | <StatusBadge status="shipped" /> | M4 |
| Trace comparison — structural diff of two traces | <StatusBadge status="shipped" /> | M4 |
| Logs — full-text search, severity/service filters, `trace_id` correlation | <StatusBadge status="shipped" /> | M1 |
| System Status — component health, storage, retention, disk | <StatusBadge status="shipped" /> | M1 |
| Sensor DaemonSet — **zero-code** eBPF traces + RED (OBI), **zero-config** log collection | <StatusBadge status="shipped" /> | M2 |
| Service inventory — sortable RED table, drill-down to traces | <StatusBadge status="shipped" /> | M2 |
| RED metrics dashboard — rate/errors/duration charts per service | <StatusBadge status="shipped" /> | M3 |
| Node / pod health — CPU, memory, network per node & pod (kubeletstats), sortable on every column and filterable by name, namespace or workload | <StatusBadge status="shipped" /> | M3 |
| Continuous profiling — CPU flame graphs per service (experimental, opt-in) | <StatusBadge status="shipped" /> | M4 |
| Projects — per-environment scoping, sidebar switcher, shareable `?project=` links | <StatusBadge status="shipped" /> | v0.1 |
| Project management — create, rename and delete projects from the UI; `default` and config-defined projects stay read-only | <StatusBadge status="shipped" /> | v0.3 |
| Read-only demo — one-click "Try the demo" viewer backed by live data (OpenTelemetry Astronomy Shop); opt-in, server-side sign-in | <StatusBadge status="shipped" /> | v0.3 |
| Member projects — one project reads the union of several clusters on every screen; membership is one level deep and each viewer still sees only the members they were granted | <StatusBadge status="shipped" /> | v0.6 |
| Per-project retention — give a project a shorter window than the install, enforced by an hourly scoped trim | <StatusBadge status="shipped" /> | v0.6 |
| Per-project storage usage — rows, estimated size, ingest rate and effective retention for the selected project, beside the instance-wide totals | <StatusBadge status="shipped" /> | v0.6 |
| Component toggles — install the ingest half alone on a secondary cluster (`hub.enabled` / `ui.enabled` / `gateway.enabled`), writing to a shared store | <StatusBadge status="shipped" /> | v0.6 |
| Collection controls — per signal/namespace/pod/node, with a per-node agent inventory | <StatusBadge status="shipped" /> | v0.1 |
| Runtime collection control — switch each signal on or off from Settings → Collection and the sensor follows in seconds, no `helm upgrade`; shows the effective config and resets to chart defaults. Opt-in, behind a namespace-scoped Role over the hub's own sensor resources | <StatusBadge status="shipped" /> | v0.5 |
| Error tracking — deduplicated issues from spans/logs, triage lifecycle, Sentry-protocol ingest | <StatusBadge status="shipped" /> | v0.2 |
| Service health — group health with criticality tiers and critical-dependency propagation, derived from RED | <StatusBadge status="shipped" /> | v0.2 |
| Service groups from the UI — create, edit and delete health groups in Settings → Groups; applies to the next health read, no `helm upgrade`. Chart-declared groups stay read-only and win a name collision; auto-grouping by namespace is unchanged | <StatusBadge status="shipped" /> | v0.5 |
| Alerting — webhook notifications when a service/group crosses into a bad state | <StatusBadge status="shipped" /> | v0.2 |
| Network health on the service map — per-edge RTT + failed/reset connections (OBI TCP stats) | <StatusBadge status="shipped" /> | v0.2 |
| Green — per-service energy (Wh) & carbon (gCO2e) via CNCF Kepler, carbon budgets, CSRD-ready export (off by default; measured on RAPL hardware) | <StatusBadge status="shipped" /> | v0.2 |
| Sensor safety — CI-proven "do no harm" gate (probe-sensitive canary) + opt-in instrumentation mode (`discovery.mode`) | <StatusBadge status="shipped" /> | v0.2 |
| Authentication — secure by default: local users, fixed roles (admin/editor/viewer) with per-project grants, server-side sessions, Settings → Users, opt-in anonymous viewer | <StatusBadge status="shipped" /> | v0.2 |
| SSO — OpenID Connect (discovery, PKCE) with any IdP, group→role/project mapping (hot-reloaded), `forceSSO` | <StatusBadge status="shipped" /> | v0.2 |
| Modules — one switch per signal family (`modules.<name>.enabled`) gates schema, API, pipeline, collection and UI together; capabilities-driven sidebar | <StatusBadge status="shipped" /> | v0.2 |
| Ingest API keys — per-project keys validated in the gateway; `enforce` makes the key's project the authoritative tenant; safe `off`/`log`/`enforce` rollout | <StatusBadge status="shipped" /> | v0.3 |
| Green on RAPL-less nodes — opt-in TDP power estimation for cloud VMs, labeled *estimated* end to end and never blended with measured energy; `/green` coverage panel. The measured source can be dropped entirely (`sensor.green.kepler.enabled=false`) so a RAPL-less fleet keeps its other signals | <StatusBadge status="shipped" /> | v0.3 |
| User management — edit a user's name and role grants, reset passwords, and delete users behind a disable-first rule; password operations refused for SSO accounts | <StatusBadge status="shipped" /> | v0.4 |
| Self-service password change — Settings → Account, current password required, other sessions evicted while yours stays live | <StatusBadge status="shipped" /> | v0.4 |
| Schema self-healing — the hub applies missing migrations on connect (`hub.autoMigrate`) and reports applied vs. expected as a **Schema** component in Settings → Status | <StatusBadge status="shipped" /> | v0.4 |
| Reverse-proxy support for login — `auth.trustedOrigins` plus an `enforce`/`log`/`off` origin check for proxies that rewrite `Host` | <StatusBadge status="shipped" /> | v0.4 |
| Storage view — ClickHouse connection (read-only), per-signal size, compression and age, and configured retention shown against the TTL the tables actually enforce | <StatusBadge status="shipped" /> | v0.5 |
| Access view — which role may read and which may change each area, derived by the hub from the guards its routes registered with, so it cannot drift from what is enforced | <StatusBadge status="shipped" /> | v0.5 |
| Dashboard — the landing screen: service-group health, compact topology, firing alerts and Kubernetes capacity in one view. Each band follows its module, and falls back to the busiest services rather than disappearing when service health is off | <StatusBadge status="shipped" /> | v0.5 |
| SSO group mapping from the UI — Settings → Access shows the chart-declared OIDC group→role rules and lets an admin author, edit and delete rules beside them; the chart wins a name collision and an overridden rule says why. A change applies on the group's next sign-in or token refresh, cluster-wide within ~15 s | <StatusBadge status="shipped" /> | v0.5 |
| Personal API tokens — mint, list and revoke bearer tokens in Settings → Access; only the SHA-256 is stored and the raw value is shown once. A token resolves to its owner's live grants, so disabling a user disables every token they hold; a bad token is a clean 401, never an anonymous downgrade | <StatusBadge status="shipped" /> | v0.5 |
| Wider ingest compatibility — Jaeger, Zipkin, Prometheus remote-write and Loki push receivers alongside OTLP, one values flag each and all off by default; every receiver goes through the same tenant stage, so ingest keys are enforced identically whatever the wire protocol. Forwarding exporters (OTLP/Kafka) dual-write to a second backend during a migration, behind a bounded queue so a dead target cannot backpressure storage | <StatusBadge status="shipped" /> | v0.6 |
| Green coverage and budget deliverability — a per-node energy table under `/green` with the measured/estimated split kept per row, carbon budgets that say whether they can actually reach a channel, and a warning when a budget targets a service group nothing rolls up to | <StatusBadge status="shipped" /> | v0.6 |
| Business tags — map a Kubernetes pod label once (`tags.labels`) and it rides every signal as `avuru.tag.<key>`, applied at collection so uninstrumented workloads carry it too; then filter traces and logs by it, with the discovered keys and values offered as controls. A trace matches when **any** service that took part carries the tag | <StatusBadge status="shipped" /> | v0.7 |
| Declared service metadata — a service states its own domain (`service.namespace`), environment and `avuru.tier` as resource attributes and the health board groups it accordingly, across Kubernetes namespaces, with no hub config. Operator config still wins, and a declaration the hub cannot use is surfaced rather than silently dropped | <StatusBadge status="shipped" /> | v0.7 |
| `avuruobs` CLI — `services`, `health`, `traces`, `logs`, `status` over the public API with a personal token, `-o table\|json`, and a `--fail-on` predicate for CI gates with three exit codes so a tripped gate is distinguishable from a broken one | <StatusBadge status="shipped" /> | v0.7 |
| Grafana data source — service RED, service health, trace search and cross-zone traffic in dashboards you already run. A backend plugin, so the API token never reaches a browser and queries leave the Grafana server | <StatusBadge status="shipped" /> | v0.7 |
| Inter-zone traffic accounting — bytes per availability-zone pair from kernel flows, standalone from the per-edge network feature so zone accounting costs zone-pair cardinality rather than workload-pair | <StatusBadge status="shipped" /> | v0.7 |
| Mesh-aware service map — transport workloads (mesh sidecars, waypoint and ztunnel proxies, ingress and egress gateways) are recognised and hidden behind a **Show mesh & gateways** toggle instead of being drawn as dependencies; flow-derived edges are drawn and counted apart from traced calls | <StatusBadge status="shipped" /> | v0.7 |
| Contextual documentation — every screen links to the page of the manual that explains it | <StatusBadge status="shipped" /> | v0.7 |

## Coming

| Capability | Status | Target |
|---|---|---|
| A richer service map — namespace and group boundaries, edge volume on the edge, distinct treatment for external and undetected peers | <StatusBadge status="planned" /> | v0.8 |
| Virtual targets — databases, caches and message brokers as first-class map nodes, derived from the exit spans already stored | <StatusBadge status="planned" /> | v0.8 |
| Layered navigation — a structure that grows with the product while keeping the first-five-minutes path short | <StatusBadge status="planned" /> | v0.8 |

:::info Service map today
Topology edges are **derived from trace spans** (cross-service Client/Server
pairs) — and with the sensor DaemonSet, every HTTP/gRPC service is traced
zero-code, so uninstrumented apps appear too. OBI's built-in **`network`
feature** further enriches the map with un-instrumented edges and per-edge
network health when enabled. A node's ring is read from the
[service-health](/docs/signals/service-health) rollup, not re-derived on the
map — a service outside that rollup's coverage reads *unknown*, never healthy.
:::
