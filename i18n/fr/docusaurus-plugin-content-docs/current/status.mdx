---
id: status
title: État des fonctionnalités
sidebar_label: État des fonctionnalités
sidebar_position: 5
description: Ce qui fonctionne dans avuru obs aujourd'hui et ce qui arrive bientôt.
---

import {StatusBadge} from '@site/src/components';

# État des fonctionnalités

Un aperçu de ce qui est disponible aujourd'hui par rapport à ce qui arrive.
avuru obs [**v0.13.0** est publiée](/docs/releases) et le tronc évolue vite —
pour le plan, voir la [Feuille de route](/docs/roadmap), et pour les
changements livrés le [Changelog](/changelog).

## Disponible maintenant

| Capacité | État | Depuis |
|---|---|---|
| Carte des services — arêtes d'appel **dérivées des traces**, anneaux de santé issus de la consolidation service-health, latence p50/p95 par arête côté appelant, survol focalisé, et filtres de recherche/statut/groupe partageables | <StatusBadge status="shipped" label="Livré" /> | M1 |
| Cibles virtuelles — les bases de données, caches et brokers dont vos services dépendent, déduits de leurs spans de sortie ; aucun agent dans la dépendance, et un broker dessiné des deux côtés | <StatusBadge status="shipped" label="Livré" /> | v0.8 |
| Frontières sur la carte — groupez le graphe par namespace Kubernetes ou par groupe de services, chacun dessiné en conteneur étiqueté | <StatusBadge status="shipped" label="Livré" /> | v0.8 |
| Pairs non détectés — l'extrémité d'une connexion que personne n'a instrumentée, dessinée au lieu d'être jetée, et comptée à part des services | <StatusBadge status="shipped" label="Livré" /> | v0.8 |
| Voisinage d'un service — les appelants et les dépendances d'un service dessinés en schéma fixe, de gauche à droite, sur son onglet Vue d'ensemble, chaque flèche portant le débit et le p95 côté appelant de ce chemin. Mêmes arêtes que les tableaux, aucune requête supplémentaire, et les mêmes refus : un saut reconstitué à travers un proxy de maillage indique `via <proxy>`, une arête non chronométrée ne porte aucune latence plutôt qu'un `0ms`, et un pair qui n'a jamais émis de span est dessiné en contour plutôt qu'en plein | <StatusBadge status="shipped" label="Livré" /> | v0.13 |
| Un assistant hébergé peut se connecter — le serveur MCP parle OAuth 2.1 : découverte, enregistrement, code d'autorisation avec PKCE, jetons de rafraîchissement à rotation. Les jetons d'accès sont opaques et liés au point d'entrée MCP : l'un d'eux ne peut jamais être rejoué contre le reste de l'API, et ce qu'il peut atteindre est relu à chaque requête. L'écran de consentement indique qu'approuver fait sortir traces et corps de journaux de l'installation, signale que le nom déclaré par l'application n'est pas vérifié, et limite l'accès à un seul projet ; Réglages → Accès liste ce que vous avez connecté et permet de le déconnecter. **Désactivé par défaut**, derrière son propre interrupteur | <StatusBadge status="shipped" /> | v0.14 |
| Mise au point sur la carte — « Voir sur la carte » conserve un service **et son voisinage à un saut** au lieu de filtrer le graphe jusqu'à un nœud isolé | <StatusBadge status="shipped" label="Livré" /> | v0.13 |
| Traces — recherche, filtres (tags, ordre, durée, statut), heatmap de latence, aperçu RED par opération | <StatusBadge status="shipped" label="Livré" /> | M1 |
| Visualiseur de traces — chronologie, table des spans, flamegraph, statistiques, graphe, JSON | <StatusBadge status="shipped" label="Livré" /> | M4 |
| Comparaison de traces — diff structurel de deux traces | <StatusBadge status="shipped" label="Livré" /> | M4 |
| Logs — recherche plein texte, filtres sévérité/service, corrélation `trace_id` | <StatusBadge status="shipped" label="Livré" /> | M1 |
| État du système — santé des composants, stockage, rétention, disque | <StatusBadge status="shipped" label="Livré" /> | M1 |
| DaemonSet sensor — traces + RED eBPF **zéro-code** (OBI), collecte de logs **zéro-config** | <StatusBadge status="shipped" label="Livré" /> | M2 |
| Inventaire des services — table RED triable, exploration vers les traces | <StatusBadge status="shipped" label="Livré" /> | M2 |
| Tableau de bord RED — graphiques débit/erreurs/durée par service | <StatusBadge status="shipped" label="Livré" /> | M3 |
| Santé des nœuds / pods — CPU, mémoire, réseau par nœud & pod (kubeletstats), triables sur chaque colonne et filtrables par nom, espace de noms ou charge de travail | <StatusBadge status="shipped" label="Livré" /> | M3 |
| Profilage continu — flame graphs CPU par service (expérimental, opt-in) | <StatusBadge status="shipped" label="Livré" /> | M4 |
| Projets — portée par environnement, sélecteur dans la barre latérale, liens `?project=` partageables | <StatusBadge status="shipped" label="Livré" /> | v0.1 |
| Gestion des projets — créer, renommer et supprimer des projets depuis l'UI ; les projets `default` et définis par configuration restent en lecture seule | <StatusBadge status="shipped" label="Livré" /> | v0.3 |
| Démo en lecture seule — lecteur « Essayer la démo » en un clic, alimenté par des données réelles (OpenTelemetry Astronomy Shop) ; opt-in, connexion côté serveur | <StatusBadge status="shipped" label="Livré" /> | v0.3 |
| Projets membres — un projet lit l'union de plusieurs clusters sur tous les écrans ; l'appartenance a un seul niveau et chacun ne voit que les membres qui lui sont attribués | <StatusBadge status="shipped" label="Livré" /> | v0.6 |
| Rétention par projet — donner à un projet une fenêtre plus courte que celle de l'installation, appliquée par un balayage horaire limité à ce projet | <StatusBadge status="shipped" label="Livré" /> | v0.6 |
| Volume de stockage par projet — lignes, taille estimée, débit d'ingestion et rétention effective du projet sélectionné, à côté des totaux de l'installation | <StatusBadge status="shipped" label="Livré" /> | v0.6 |
| Interrupteurs de composants — n'installer que la moitié « ingestion » sur un cluster secondaire (`hub.enabled` / `ui.enabled` / `gateway.enabled`), en écrivant dans un stockage partagé | <StatusBadge status="shipped" label="Livré" /> | v0.6 |
| Contrôles de collecte — par signal/namespace/pod/nœud, avec inventaire des agents par nœud | <StatusBadge status="shipped" label="Livré" /> | v0.1 |
| Collecte pilotable à chaud — activez ou coupez chaque signal depuis Réglages → Collecte et le capteur suit en quelques secondes, sans `helm upgrade` ; affiche la configuration effective et revient aux valeurs du chart. Optionnelle, derrière un rôle limité à l'espace de noms et aux ressources de capteur du hub | <StatusBadge status="shipped" label="Livré" /> | v0.5 |
| Suivi des erreurs — problèmes dédupliqués depuis spans/logs, cycle de triage, ingestion au protocole Sentry | <StatusBadge status="shipped" label="Livré" /> | v0.2 |
| Santé des services — santé de groupe avec niveaux de criticité et propagation des dépendances critiques, dérivée du RED | <StatusBadge status="shipped" label="Livré" /> | v0.2 |
| Groupes de services depuis l'interface — créez, modifiez et supprimez les groupes de santé dans Paramètres → Groupes ; effectif dès la lecture de santé suivante, sans `helm upgrade`. Les groupes déclarés dans le chart restent en lecture seule et l'emportent en cas de collision de nom ; le regroupement automatique par espace de noms est inchangé | <StatusBadge status="shipped" label="Livré" /> | v0.5 |
| Alerting — notifications par webhook quand un service/groupe passe dans un mauvais état | <StatusBadge status="shipped" label="Livré" /> | v0.2 |
| Santé réseau sur la carte des services — RTT + connexions échouées/réinitialisées par arête (stats TCP OBI) | <StatusBadge status="shipped" label="Livré" /> | v0.2 |
| Green — énergie (Wh) & carbone (gCO2e) par service via CNCF Kepler, budgets carbone, export prêt pour la CSRD (désactivé par défaut ; mesuré sur matériel RAPL) | <StatusBadge status="shipped" label="Livré" /> | v0.2 |
| Sûreté du capteur — gate CI « do no harm » (canari sensible aux probes) + mode d'instrumentation opt-in (`discovery.mode`) | <StatusBadge status="shipped" label="Livré" /> | v0.2 |
| Authentification — sécurisé par défaut : utilisateurs locaux, rôles fixes (admin/éditeur/lecteur) avec droits par projet, sessions côté serveur, Paramètres → Utilisateurs, lecteur anonyme opt-in | <StatusBadge status="shipped" label="Livré" /> | v0.2 |
| SSO — OpenID Connect (découverte, PKCE) avec n'importe quel IdP, mapping groupe→rôle/projets (rechargé à chaud), `forceSSO` | <StatusBadge status="shipped" label="Livré" /> | v0.2 |
| Modules — un interrupteur par famille de signaux (`modules.<name>.enabled`) contrôle d'un bloc schéma, API, pipeline, collecte et interface ; barre latérale pilotée par les capacités | <StatusBadge status="shipped" label="Livré" /> | v0.2 |
| Clés d'API d'ingestion — clés par projet validées dans la gateway ; en `enforce`, le projet de la clé devient le tenant qui fait autorité ; déploiement progressif `off`/`log`/`enforce` | <StatusBadge status="shipped" label="Livré" /> | v0.3 |
| Green sur les nœuds sans RAPL — estimation de puissance par TDP pour les VM cloud (optionnelle), étiquetée *estimée* de bout en bout et jamais mélangée à l'énergie mesurée ; panneau de couverture sur `/green`. La source mesurée peut être retirée entièrement (`sensor.green.kepler.enabled=false`) pour qu'une flotte sans RAPL conserve ses autres signaux | <StatusBadge status="shipped" label="Livré" /> | v0.3 |
| Gestion des utilisateurs — modifier le nom et les rôles d'un utilisateur, réinitialiser les mots de passe et supprimer un compte selon la règle « désactiver d'abord » ; opérations de mot de passe refusées aux comptes SSO | <StatusBadge status="shipped" label="Livré" /> | v0.4 |
| Changement de mot de passe en libre-service — Paramètres → Compte, mot de passe actuel exigé, autres sessions fermées pendant que la vôtre reste ouverte | <StatusBadge status="shipped" label="Livré" /> | v0.4 |
| Auto-réparation du schéma — le hub applique les migrations manquantes à la connexion (`hub.autoMigrate`) et signale l'état appliqué face à l'attendu via un composant **Schéma** dans Paramètres → État | <StatusBadge status="shipped" label="Livré" /> | v0.4 |
| Connexion derrière un reverse proxy — `auth.trustedOrigins` et un contrôle d'origine `enforce`/`log`/`off` pour les proxys qui réécrivent `Host` | <StatusBadge status="shipped" label="Livré" /> | v0.4 |
| Vue Stockage — connexion ClickHouse (lecture seule), taille, compression et ancienneté par signal, et rétention configurée confrontée au TTL réellement appliqué par les tables | <StatusBadge status="shipped" label="Livré" /> | v0.5 |
| Vue Accès — quel rôle peut lire et lequel peut modifier chaque domaine, déduit par le hub des gardes avec lesquelles ses routes sont enregistrées : impossible de diverger de ce qui est appliqué | <StatusBadge status="shipped" label="Livré" /> | v0.5 |
| Tableau de bord — l'écran d'accueil : santé des groupes de services, topologie compacte, alertes actives et capacité Kubernetes en une seule vue. Chaque bande suit son module et se rabat sur les services les plus actifs, au lieu de disparaître, quand la santé des services est désactivée | <StatusBadge status="shipped" label="Livré" /> | v0.5 |
| Mapping des groupes SSO depuis l'interface — Paramètres → Accès affiche les règles OIDC groupe→rôle déclarées dans le chart et permet à un administrateur d'en créer, modifier et supprimer d'autres à côté ; le chart l'emporte en cas de collision de nom et une règle éclipsée dit pourquoi. Un changement s'applique à la prochaine connexion du groupe, sur tout le cluster en ~15 s | <StatusBadge status="shipped" label="Livré" /> | v0.5 |
| Jetons d'API personnels — émettez, listez et révoquez des jetons bearer dans Paramètres → Accès ; seul le SHA-256 est stocké et la valeur brute n'est montrée qu'une fois. Un jeton est résolu vers les droits vivants de son propriétaire : désactiver un utilisateur désactive tous ses jetons ; un jeton invalide est un 401 net, jamais une rétrogradation anonyme | <StatusBadge status="shipped" label="Livré" /> | v0.5 |
| Compatibilité d'ingestion élargie — récepteurs Jaeger, Zipkin, Prometheus remote-write et Loki push en plus d'OTLP, un flag de values chacun et tous désactivés par défaut ; chaque récepteur passe par la même étape de tenant, donc les clés d'ingestion s'appliquent à l'identique quel que soit le protocole. Des exporteurs de transfert (OTLP/Kafka) dupliquent les écritures vers un second backend pendant une migration, derrière une file bornée pour qu'une cible morte n'exerce aucune contre-pression sur le stockage | <StatusBadge status="shipped" label="Livré" /> | v0.6 |
| Couverture green et délivrabilité des budgets — un tableau d'énergie par nœud sur `/green`, la distinction mesuré/estimé conservée ligne par ligne, des budgets carbone qui disent s'ils peuvent réellement joindre un canal, et un avertissement quand un budget vise un groupe de services auquel rien ne se rattache | <StatusBadge status="shipped" label="Livré" /> | v0.6 |

| Étiquettes métier — associez une fois un label de pod Kubernetes (`tags.labels`) et il accompagne chaque signal sous la forme `avuru.tag.<clé>`, appliqué à la collecte pour que les charges non instrumentées le portent aussi ; filtrez ensuite traces et journaux dessus, les clés et valeurs découvertes étant proposées comme contrôles. Une trace correspond dès qu'**un** service ayant participé porte l'étiquette | <StatusBadge status="shipped" label="Livré" /> | v0.7 |
| Métadonnées déclarées par le service — un service annonce lui-même son domaine (`service.namespace`), son environnement et son `avuru.tier` en attributs de ressource, et le tableau de santé le regroupe en conséquence, à travers les namespaces Kubernetes et sans configuration côté hub. La configuration de l'opérateur l'emporte toujours, et une déclaration inutilisable est signalée plutôt qu'ignorée en silence | <StatusBadge status="shipped" label="Livré" /> | v0.7 |
| CLI `avuruobs` — `services`, `health`, `traces`, `logs`, `status` via l'API publique avec un jeton personnel, `-o table\|json`, et un prédicat `--fail-on` pour les garde-fous de CI, avec trois codes de sortie pour distinguer un garde-fou déclenché d'un garde-fou en panne | <StatusBadge status="shipped" label="Livré" /> | v0.7 |
| Source de données Grafana — RED par service, santé des services, recherche de traces et trafic inter-zones dans les tableaux de bord que vous exploitez déjà. Un plugin backend : le jeton d'API n'atteint jamais le navigateur et les requêtes partent du serveur Grafana | <StatusBadge status="shipped" label="Livré" /> | v0.7 |
| Comptabilité du trafic inter-zones — octets par paire de zones de disponibilité depuis les flux noyau, indépendante de la fonctionnalité réseau par arête : la comptabilité coûte le cardinal des paires de zones, pas celui des paires de charges de travail | <StatusBadge status="shipped" label="Livré" /> | v0.7 |
| Carte des services consciente du maillage — les charges de transport (sidecars de maillage, proxys waypoint et ztunnel, passerelles d'entrée et de sortie) sont reconnues et masquées derrière une bascule **Afficher maillage et passerelles** au lieu d'être dessinées comme des dépendances ; les arêtes issues des flux sont tracées et comptées à part des appels tracés | <StatusBadge status="shipped" label="Livré" /> | v0.7 |
| Documentation contextuelle — chaque écran renvoie à la page du manuel qui l'explique | <StatusBadge status="shipped" label="Livré" /> | v0.7 |
| Une carte des services plus riche — frontières de namespace et de groupe de services, volume porté par chaque arête à la demande, pairs non détectés dessinés au lieu d'être jetés, une légende qui explique chaque canal utilisé et un indicateur de zoom | <StatusBadge status="shipped" label="Livré" /> | v0.8 |
| Cibles virtuelles — bases de données, caches et courtiers de messages comme nœuds à part entière de la carte, dérivés des spans sortants déjà stockés. Un courtier est dessiné par ses deux extrémités : une file n'est jamais une impasse | <StatusBadge status="shipped" label="Livré" /> | v0.8 |
| Navigation par couches — barre latérale groupée par la question à laquelle chaque écran répond (Topologie, Signaux, Opérations, Infrastructure), le chemin des cinq premières minutes inchangé, et une couche dont tous les écrans sont inactifs disparaît au lieu d'afficher un manque | <StatusBadge status="shipped" label="Livré" /> | v0.8 |
| Reconstitution du saut de proxy — la vraie dépendance `app → app` retrouvée à travers jusqu'à trois proxys enchaînés, en remontant la parenté propre à chaque trace, et nommée avec le proxy traversé. La bascule **Afficher maillage et passerelles** échange les représentations au lieu de les empiler : une requête n'est jamais dessinée deux fois | <StatusBadge status="shipped" label="Livré" /> | v0.9 |
| Surfaces dédiées au maillage — charge, latence et taux de succès par proxy, avec les appels transportés en entrée et en sortie comptés séparément, plus la santé du plan de contrôle : proxys connectés, convergence des poussées, et la configuration que vos proxys ont refusée. Sans collecte, l'écran le dit au lieu d'afficher zéro | <StatusBadge status="shipped" label="Livré" /> | v0.9 |
| Retransmissions TCP sur les arêtes de la carte — la perte de paquets sur un lien, que le RTT seul ne révèle pas, aux côtés du RTT p95 et des connexions échouées ; une arête qui retransmet est signalée dégradée pour cette seule raison | <StatusBadge status="shipped" label="Livré" /> | v0.9 |
| Attribution réseau par arête, vérifiée en intégration continue sur un vrai cluster eBPF plutôt que supposée — ce qui a révélé et corrigé un plantage latent emportant toute la sonde dès que les statistiques TCP étaient activées | <StatusBadge status="shipped" label="Livré" /> | v0.9 |
| Sondes de point d'entrée — requêtes planifiées rattachées à un groupe de santé, pour distinguer un groupe sans trafic d'un groupe sans service. Deux échecs consécutifs font bouger un groupe, jamais un seul, et chaque sonde émet son propre span : une sonde en échec renvoie vers la trace de la requête qui a échoué | <StatusBadge status="shipped" label="Livré" /> | v0.9 |
| Coûts & gaspillage — le CPU et la mémoire réservés par chaque charge de travail face à ce qu'elle a réellement consommé, classés par l'écart, avec les charges qui ne déclarent **aucune** requête signalées comme un état à part entière et l'allocation d'un nœud affichée à côté de son usage. L'inactif se mesure face au pic, jamais à la moyenne. Les tarifs sont les vôtres à déclarer ; sans eux, les écrans affichent des cœurs et des octets et le disent — aucune API de tarification, aucune sortie de données. Désactivé par défaut | <StatusBadge status="shipped" label="Livré" /> | v0.10 |
| Transport reconnu par les labels Kubernetes — une passerelle que vous avez nommée n'importe comment est classée grâce au label que votre maillage lui a posé, le nom restant la réponse pour les sidecars (qui portent les labels de leur application) et votre liste `applications` restant décisive | <StatusBadge status="shipped" label="Livré" /> | v0.10 |
| Silence du plan de contrôle expliqué — rien ne le collecte, la cible ne répond pas, ou elle a répondu avec des métriques qui ne sont pas celles d'un plan de contrôle que ce produit sait lire. Trois états et trois corrections différentes, là où il n'y avait qu'une phrase | <StatusBadge status="shipped" label="Livré" /> | v0.10 |
| Observabilité IA — les appels de modèles que vos applications envoient déjà : par modèle, les appels, les tokens en entrée et en sortie, la latence, les échecs et les troncatures ; par service appelant, les mêmes chiffres avec un propriétaire. Le modèle qui a **répondu** l'emporte sur celui demandé, les deux orthographes des tokens sont lues, un appel sans usage rapporté est exclu plutôt que compté à zéro, et la troncature est signalée à part de l'échec. Les tarifs sont les vôtres à déclarer ; il n'y a pas d'API de tarification. Désactivé par défaut | <StatusBadge status="shipped" label="Livré" /> | v0.11 |
| Prompts et complétions supprimés à la passerelle par défaut — le contenu des messages n'atteint le stockage que parce que le SDK d'une application l'a capturé, et il y était jusqu'ici conservé avec la rétention habituelle et montré à tout Viewer. Ancré pour qu'un *compteur* de tokens ne soit jamais pris pour un prompt, et délibérément **non** conditionné au module IA | <StatusBadge status="shipped" label="Livré" /> | v0.11 |
| Répartition des traces — le trafic en treemap et en anneau, groupé par service, opération, issue, type de span ou n'importe quel attribut de span/ressource, pondéré par nombre de requêtes ou par temps total, sur une population de spans explicite (servi / entré / tous les spans). La queue est une vraie catégorie, donc les parts composent le tout | <StatusBadge status="shipped" label="Livré" /> | v0.11 |
| Page de détail par service — la santé d'un service, son RED dans le temps, ses appelants et ses dépendances en deux listes séparées, et ses traces, journaux et incidents derrière des onglets. Composée de lectures qui existaient déjà | <StatusBadge status="shipped" label="Livré" /> | v0.11 |
| Chemin de trace — le graphe au niveau service d'une seule requête, pondéré par le temps passé *dans* chaque service plutôt que par la durée du span, avec les dépendances qui n'ont jamais émis dessinées comme les sauts terminaux qu'elles sont | <StatusBadge status="shipped" label="Livré" /> | v0.11 |
| Refusé — le 4xx côté serveur comme issue à part entière, à côté de ok et erreur, sur le badge du span, l'aperçu des opérations, la table des traces et le filtre de recherche, et délibérément tenu hors du taux d'erreur | <StatusBadge status="shipped" label="Livré" /> | v0.11 |
| Appels d'outils distingués des appels de modèles — le module IA vérifiait la *présence* de `gen_ai.operation.name` sans jamais en lire la valeur : sur une charge agentique, chaque span `execute_tool` comptait comme un appel de modèle. Le nombre d'appels gonflait, la latence mêlait une requête en base à une complétion, le modèle ne se résolvait plus et le compartiment « aucun usage » se remplissait de spans qui n'en étaient pas. Les embeddings restent comptés comme des appels de modèles : ils consomment de vrais tokens | <StatusBadge status="shipped" label="Livré" /> | v0.12 |
| Table des outils — par outil exécuté par un agent : appels, échecs, p95 et services appelants, un outil sollicité quatre fois formant une seule ligne avec un compteur. Ni tokens ni coût : une exécution d'outil n'en consomme pas, et un zéro se lirait « gratuit » | <StatusBadge status="shipped" label="Livré" /> | v0.12 |
| Vue Tour d'agent — un tour dessiné comme le graphe qu'il est, appels de modèles et exécutions d'outils comme types de nœuds distincts, pondérés par le temps passé *à l'intérieur* de chaque appel plutôt que par la durée du span. Proposée uniquement sur les traces qui en contiennent un | <StatusBadge status="shipped" label="Livré" /> | v0.12 |
| Budgets de dépense — plafonds mensuels en tokens ou en monnaie, par service appelant ou pour tout le parc, déclenchés via vos canaux d'alerting existants. Un budget en monnaie sans prix déclaré est refusé au démarrage plutôt que de rester éternellement sous tous les seuils, et un périmètre partiellement tarifé indique que son chiffre est un plancher | <StatusBadge status="shipped" label="Livré" /> | v0.12 |
| Une seule grille tarifaire — prix des modèles et tarifs de calcul dans un même document avec une seule devise, éditable dans les Paramètres et appliquée sans redéploiement. Les valeurs déclarées dans le chart restent lisibles et en lecture seule, les entrées de l'interface s'y superposent, et chaque ligne indique son origine | <StatusBadge status="shipped" label="Livré" /> | v0.12 |
| Serveur MCP — six outils en lecture seule sur les traces, les logs, les issues et la santé déjà stockés, pour qu'un agent enquête sur un incident au lieu qu'une personne retape un écran. Un seul handler sur le hub, authentifié par les jetons d'API personnels qui résolvent les permissions vivantes de leur propriétaire. Un service mal orthographié renvoie les correspondances les plus proches plutôt qu'un résultat vide, et une section à laquelle l'installation ne peut pas répondre est nommée plutôt que silencieusement absente. **Éteint par défaut** : ce qu'un agent lit sort de votre cluster vers le fournisseur de modèle choisi, l'interrupteur est donc le vôtre — et chaque appel d'outil est journalisé avec le propriétaire du jeton, l'outil, ses arguments et le nombre de lignes | <StatusBadge status="shipped" label="Livré" /> | v0.12 |
| Des images de collecteur sans Critical corrigeable — la distro de la gateway épingle `x/crypto`, `x/text` et `grpc` au-dessus de leurs avis de sécurité, et l'agent de nœud suit la ligne du collecteur qui scanne le plus proprement. Un registre appliquant une policy « blocage sur Critical » cesse de servir une image signalée, ce qui se manifeste par un déploiement qui expire plutôt que par un rapport de scan | <StatusBadge status="shipped" label="Livré" /> | v0.12 |
| Aucune image n'a besoin d'exception dans un registre — l'agent de nœud reçoit sa propre distribution de collecteur, ne portant exactement que les composants utilisés par sa configuration rendue et fixant `x/crypto` et `x/mod` là où aucune version du collecteur ne le fait. Chaque image tirée par défaut par le chart est exempte de vulnérabilité critique ou haute corrigeable ; l'image amont d'origine reste disponible en surcharge, et le fichier de valeurs dit ce que cela coûte | <StatusBadge status="shipped" label="Livré" /> | v0.13 |

## À venir

| Capacité | État | Cible |
|---|---|---|
| Plans de contrôle autres qu'Istio — la moitié « par proxy » fonctionne déjà partout ; la moitié « plan de contrôle » est aujourd'hui façonnée pour Istio | <StatusBadge status="planned" label="Prévu" /> | prochaine |
| Coût joint au module vert — la même capacité réservée et inutilisée en Wh et gCO2e, sur une installation qui exécute les deux | <StatusBadge status="planned" label="Prévu" /> | prochaine |

:::info La carte des services aujourd'hui
Les arêtes de topologie sont **dérivées des spans de traces** (paires
Client/Server inter-services) — et avec le DaemonSet sensor, chaque service
HTTP/gRPC est tracé zéro-code : les applications non instrumentées apparaissent
aussi. La fonctionnalité **`network`** intégrée d'OBI enrichit encore la carte
avec les arêtes non instrumentées et la santé réseau par arête lorsqu'elle est
activée. L'anneau d'un nœud est lu depuis la consolidation
[service-health](/docs/signals/service-health), jamais recalculé sur la
carte — un service hors de la couverture de cette consolidation s'affiche
*unknown*, jamais healthy.
:::
